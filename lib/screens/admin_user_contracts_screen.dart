import 'package:flutter/material.dart';

class AdminUserContractsScreen extends StatelessWidget {
  const AdminUserContractsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Договора'),
      ),
      body: const Center(
        child: Text('Экран договоров будет добавлен позже.'),
      ),
    );
  }
}